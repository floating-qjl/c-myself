//用户登录网站，通常需要注册，一般需要输入两遍密码。请编程判断输入的两次密码是否一致，一致输出“same”，不一致输出“different”





#include <stdio.h>
#include <string.h>
int main()
{
    char a[100]={0};
    char b[100]={0};
    scanf("%s",&a);
    scanf("%s",&b);
    if(strcmp(a,b)==0)
        printf("same\n");
    else
        printf("different\n");
    return 0;
}
