//KiKi学习了循环，BoBo老师给他出了一系列打印图案的练习，该任务是打印用“*”组成的线段图案 

#include <stdio.h>

int main()
{
    int a=0;
    while(scanf("%d",&a)!=EOF)
    {
        int b=0;
        for(b=0;b<a;b++)
        {
            printf("*");
        }
        printf("\n");
    }
    return 0;
}
