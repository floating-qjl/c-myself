//任意输入一个正整数N，统计1~N之间奇数的个数和偶数的个数，并输出。

#include <stdio.h>

int main()
{
    int a=0;
    scanf("%d",&a);
    if(a%2==1)
        printf("%d %d",a/2+1,a/2);
    else
        printf("%d %d",a/2,a/2);
    return 0;
}
