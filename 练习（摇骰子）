疫情封校，让生活失去了很多色彩。游小非想找些事情在闲暇之余打发无聊的时光。现在游小非手上有 66 个骰子，每个骰子都有它的初始状态
（即它们朝上一面的点数），TaTa 一把抓起这些骰子，会摇出另一套结果。假设游小非摇骰子的技术非常高超，每次摇出的结果都满足以下两个条件：

11. 每个骰子摇出的点数都跟它之前任何一次出现的点数不同；

22. 在满足条件 11 的前提下，每次都能让每个骰子得到可能得到的最大点数。

游小非想要知道，第 nn 次摇出的结果会是多少呢？










#include <stdio.h>
int arr[6][7];
int main()
{
    int n;
    for(int a=0;a<6;++a)
    {
        scanf("%d",&n);
        ++arr[a][n];
    }
    scanf("%d",&n);
    for(int a=0;a<6;++a)
    {
        int num=0;
        for(int b=6;b>=1;--b)
        {
            if(arr[a][b]==0)
              ++num;
            if(num==n)
            {
                printf("%d ",b);
                break;
            }
        }
    }
    return 0;
}
