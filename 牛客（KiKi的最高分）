//KiKi知道了大一三科比较重要课程的分数，包括：程序设计基础，高数，英语，请编程帮他找到三科中的最高分。



#include <stdio.h>
int main()
{
    int a,b;
    int max=0;
    for(b=0;b<3;b++)
    {
        scanf("%d",&a);
        if(max<a)
            max=a;
    }
    printf("%d",max);
    return 0;
}
