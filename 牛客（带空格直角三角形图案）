//KiKi学习了循环，BoBo老师给他出了一系列打印图案的练习，该任务是打印用“*”组成的带空格直角三角形图案


#include <stdio.h>

int main()
{
    int a = 0;
    int b = 0;
    int n = 0;
    while(~scanf("%d", &n))
    {
    for (a = 0;a < n;a++)
    {
        for (b = 0;b < n - 1 - a;b++)
        {
            printf("  ");
        }
        for (b = 0;b <= a;b++)
        {
            printf("* ");
        }
        printf("\n");
    }
    }
    return 0;
}
