//KiKi想完成字母大小写转换，有一个字符，判断它是否为大写字母，
//如果是，将它转换成小写字母；反之则转换为大写字母。

#include <stdio.h>

int main()
{
    char a=0;
    while(scanf("%c\n",&a)!=EOF)
    {
        if(a>='a'&&a<='z')
        {
            printf("%c\n",a-32);
        }
        else if(a>='A'&&a<='Z')
        {
            printf("%c\n",a+32);
        }
            
    }
    return 0;
}
