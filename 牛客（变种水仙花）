//变种水仙花数 - Lily Number：把任意的数字，从中间拆分成两个数字，
//比如1461 可以拆分成（1和461）,（14和61）,（146和1),如果所有拆分后的乘积之和等于自身，则是一个Lily Number。

#include <stdio.h>
int main()
{
    int a = 0;
    for (a = 10000;a < 100000;a++)
    {
        int b = (a % 10) * (a / 10) + (a % 100) * (a / 100) + (a % 1000) * (a / 1000) + (a % 10000) * (a / 10000);
        if (a == b)
        {
            printf("%d ", a);
        }
    }
    return 0;
}
