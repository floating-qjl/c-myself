#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main()
{
	int a = 0;
	int b = 1;
	int c = 1;
	int d = 1;
	int n = 0;
	scanf("%d", &n);
	for (a = 1;a <= n;a++)
	{	
		if (a <= 2)
			printf("1 ");
		else
		{
			d = b + c;
			b = c;
			c = d;
			printf("%d ", d);
		}
	}
	system("pause");
	return 0;
}
