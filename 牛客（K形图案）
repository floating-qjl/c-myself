//KiKi学习了循环，BoBo老师给他出了一系列打印图案的练习，该任务是打印用“*”组成的K形图案。


#include <stdio.h>

int main()
{
    int a = 0;
    while (~scanf("%d", &a))
    {
        int b = 0;
        int c = 0;
        for (b = 0;b < a ;b++)
        {
            for (c = 0;c < a + 1 - b;c++)
            {
                printf("* ");
            }
            printf("\n");
        }
        for (b = 0;b < a+1;b++)
        {
            for (c = 0;c <= b;c++)
            {
                printf("* ");
            }
            printf("\n");
        }
    }
    return 0;
}
