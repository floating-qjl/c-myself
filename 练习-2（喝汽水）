#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>

int main()
{
	int money = 0;
	int totle = 0;
	int empty = 0;
	scanf("%d", &money);
	totle = money;
	empty = money;
	while (empty >= 2)
	{
		totle += empty / 2;
		empty = empty / 2 + empty % 2;
	}
	printf("%d\n", totle);

	return 0;
}
