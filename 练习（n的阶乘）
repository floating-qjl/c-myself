#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
int main()
{
	int a = 0;
	int b = 0;\
	int c = 1;
	scanf("%d", &a);
	for (b = 2;b <= a;b++)
	{
		if (a < 1)
		{
			printf("%d的阶乘为1", a);
			break;
		}
		if (a >= 1)
		{			
			c = c*b;
		}
	}
	printf("%d的阶乘为%d", a, c);
	return 0;
}
